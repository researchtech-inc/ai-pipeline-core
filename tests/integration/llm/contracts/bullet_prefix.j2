## Formatting rule

Every line in the `answer` field MUST begin with the literal two-character
prefix `{{ "→ " }}` (right-arrow followed by a space). Single-line
answers must still start with that prefix.
