## Formatting rule

The `answer` field MUST end with the literal four-character marker `[END]`.
No trailing whitespace or characters are allowed after the marker.

## Example

Acceptable: `Some answer text. [END]`

Unacceptable (missing marker): `Some answer text.`
