## Required static phrase

You MUST include the literal phrase `STATIC-PHRASE-OMEGA` verbatim somewhere
in the `answer` field. This phrase is read from a static markdown body file
and is not derivable from common knowledge.

## Task

Answer the user's question briefly, ensuring the phrase above appears in the
`answer` field exactly once. Do not transliterate or paraphrase the phrase.
