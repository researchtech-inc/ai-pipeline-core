## Task

Call the lookup tool four times in a row with four different query strings
(any short tokens of your choosing), then write a brief summary of what
happened into the `notes` field. The framework enforces a hard cap on the
number of tool calls — do not assume every requested call will succeed.
