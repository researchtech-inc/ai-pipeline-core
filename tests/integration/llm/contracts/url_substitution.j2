## Task

You will receive a source document (visible as a `<document>` context
message above) that contains one or more long URLs. Echo back EXACTLY ONE
of those URLs verbatim in the `echoed_url` field. Do not shorten,
abbreviate, or modify the URL — copy it character for character from the
supplied document.
